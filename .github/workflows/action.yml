name: "Push to actions"
on:
  push:
    branches:
      - master
    tags-ignore:
      - '*'

jobs:
  push_actions:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Checkout curse-locale-action
        uses: actions/checkout@v4
        with:
          repository: DengSir/curse-locale-action
          path: actions/curse-locale-action
          token: ${{ secrets.PAT }}

      - name: Checkout curse-publish-action
        uses: actions/checkout@v4
        with:
          repository: DengSir/curse-publish-action
          path: actions/curse-publish-action
          token: ${{ secrets.PAT }}

      - name: Checkout curse-update-action
        uses: actions/checkout@v4
        with:
          repository: DengSir/curse-update-action
          path: actions/curse-update-action
          token: ${{ secrets.PAT }}

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: npm install
        run: npm install

      - name: Build actions
        run: npm run action

      - name: Commit curse-locale-action
        uses: EndBug/add-and-commit@v9
        with:
          cwd: actions/curse-locale-action
          message: "auto build"

      - name: Commit curse-publish-action
        uses: EndBug/add-and-commit@v9
        with:
          cwd: actions/curse-publish-action
          message: "auto build"

      - name: Commit curse-update-action
        uses: EndBug/add-and-commit@v9
        with:
          cwd: actions/curse-update-action
          message: "auto build"

